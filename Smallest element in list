import heapq
def find_smallest(numbers):
    heap = [(x, x) for x in numbers]
    heapq.heapify(heap)
    smallest = heapq.heappop(heap)
    return smallest
numbers = [5, 7, 9, 14, 10, 20, 4] 
print(find_smallest(numbers))  
